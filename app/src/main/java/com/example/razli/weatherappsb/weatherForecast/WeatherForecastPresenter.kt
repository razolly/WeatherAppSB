package com.example.razli.weatherappsb.weatherForecast

class WeatherForecastPresenter(private val view: WeatherForecastContract.View)
    : WeatherForecastContract.Presenter {

    override fun start() {
        // todo initialize the Repository singleton?
    }
}